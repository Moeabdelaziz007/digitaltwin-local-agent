{
  "name": "memory-curator",
  "version": "1.1.0",
  "description": "Prunes, merges, and archives facts to prevent persona drift and memory clutter.",
  "when_to_use": "When the agent detects redundant or contradictory user facts, or during periodic memory maintenance cycles.",
  "permissions": ["memory_read", "memory_write"],
  "required_tools": ["fetch_facts", "update_fact", "archive_fact"],
  "input_schema": {
    "type": "object",
    "properties": {
      "focus_area": { "type": "string" }
    }
  },
  "output_schema": {
    "type": "object",
    "properties": {
      "actions_taken": { "type": "integer" },
      "merges": { "type": "array" }
    }
  },
  "safety_notes": "Never archive core biographical data without explicit validation."
}
